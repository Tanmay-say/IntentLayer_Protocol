{
  "name": "@intentlayer/axl-mock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "In-process HTTP mock of the Gensyn AXL Go binary. Exposes /send, /receive, /topology on configurable ports for dev/CI when the real Go binary is unavailable.",
  "scripts": {
    "start": "tsx src/server.ts",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "express": "^4.21.0",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
